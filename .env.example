# Copy this file to .env and fill in the values
# Example for Gmail (you must use an app password if 2FA is enabled)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-smtp-user@example.com
SMTP_PASS=your-smtp-password-or-app-password

# The recipient address for contact form messages (default: arushsharma0215@gmail.com)
TARGET_EMAIL=arushsharma0215@gmail.com

# Optional: override API URL used by the frontend (default is http://localhost:3001)
REACT_APP_EMAIL_API_URL=http://localhost:3001/api/send-email
